<Window x:Class="VPet_AIGF.VoiceEnrollWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🎤 声纹注册" Width="460" Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFF5F5F5"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="RoundBtn" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <StackPanel Margin="28,20,28,20">

        <!-- 标题 -->
        <TextBlock Text="🎤 声纹注册" FontSize="20" FontWeight="Bold"
                   Foreground="#5C3D8F" HorizontalAlignment="Center" Margin="0,0,0,6"/>
        <TextBlock TextWrapping="Wrap" TextAlignment="Center" FontSize="13"
                   Foreground="#888" Margin="0,0,0,18"
                   Text="录入你的声音后，只有你叫宠物名字才能唤醒它。&#10;请依次朗读下面三句话，每句录制 3 秒。"/>

        <!-- 进度提示 -->
        <Border Background="#EDE7F6" CornerRadius="8" Padding="12,10" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="tbStep" FontSize="14" FontWeight="Bold"
                           Foreground="#5C3D8F" HorizontalAlignment="Center"
                           Text="准备好后点击开始录制"/>
                <TextBlock x:Name="tbPrompt" FontSize="15" FontWeight="Bold"
                           Foreground="#333" HorizontalAlignment="Center"
                           Margin="0,8,0,0" TextWrapping="Wrap" TextAlignment="Center"
                           Text=""/>
            </StackPanel>
        </Border>

        <!-- 音量指示条 -->
        <Border Background="#E0E0E0" Height="16" CornerRadius="8" Margin="0,0,0,4"
                HorizontalAlignment="Stretch">
            <Border x:Name="levelBar" Background="#7E57C2" CornerRadius="8"
                    HorizontalAlignment="Left" Width="0" Height="16"/>
        </Border>
        <TextBlock x:Name="tbLevelHint" Text="音量" FontSize="11"
                   Foreground="#AAA" HorizontalAlignment="Center" Margin="0,0,0,12"/>

        <!-- 步骤指示 -->
        <Grid Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Border x:Name="dotStep1" Grid.Column="0" Background="#B39DDB"
                    CornerRadius="4" Height="6" Margin="4,0"/>
            <Border x:Name="dotStep2" Grid.Column="1" Background="#D1C4E9"
                    CornerRadius="4" Height="6" Margin="4,0"/>
            <Border x:Name="dotStep3" Grid.Column="2" Background="#D1C4E9"
                    CornerRadius="4" Height="6" Margin="4,0"/>
        </Grid>

        <!-- 操作按钮 -->
        <Button x:Name="btnRecord" Content="🎙️ 开始录制第 1 句"
                Style="{StaticResource RoundBtn}"
                Background="#7E57C2" Foreground="White"
                FontSize="15" HorizontalAlignment="Stretch"
                Height="44" Margin="0,0,0,10"
                Click="BtnRecord_Click"/>

        <Button x:Name="btnClear" Content="🗑️ 清除声纹重新注册"
                Style="{StaticResource RoundBtn}"
                Background="#EF9A9A" Foreground="White"
                HorizontalAlignment="Stretch" Height="36"
                Margin="0,0,0,10"
                Click="BtnClear_Click"/>

        <!-- 状态文字 -->
        <TextBlock x:Name="tbStatus" FontSize="13" TextWrapping="Wrap"
                   TextAlignment="Center" Foreground="#666"
                   Margin="0,4,0,0" MinHeight="40"/>

    </StackPanel>
</Window>

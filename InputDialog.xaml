<Window x:Class="VPet_AIGF.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Height="150" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#FFF0F0F5">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbPrompt" Text="请输入：" Margin="0,0,0,10"/>
        <TextBox x:Name="txtInput" Grid.Row="1" Margin="0,0,0,10" Padding="4"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="确定" Width="60" Margin="0,0,10,0" Click="btnOk_Click"/>
            <Button Content="取消" Width="60" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>